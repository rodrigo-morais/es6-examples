'use strict';

var template = function(x, y, z){
    let color = "white";
    let size = "big";

    console.log("The cat is ${color} and ${size}.");
    console.log(`The cat is ${color} and ${size}.`);
};

template();